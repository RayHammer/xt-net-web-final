@using Final.PL.Models;
@{
    Page.Title = "Login";
    Layout = "~/Layouts/_MainLayout.cshtml";

    if (IsPost)
    {
        string username = Request["username"];
        string password = Request["password"];

        if (AuthModel.Authorize(username, password))
        {
            FormsAuthentication.SetAuthCookie(username,
                createPersistentCookie: true);
        }

        Response.Redirect("~");
    }
    else
    {
        if (Convert.ToBoolean(Request["logout"]))
        {
            FormsAuthentication.SignOut();
            Response.Redirect("~");
        }
    }
}

<div class="container">
    <h3>Login</h3>
    <form action="~/Pages/Login" method="post">
        <input type="text" name="Username" placeholder="Login" class="form-control mb-3" />
        <input type="password" name="Password" placeholder="Password" class="form-control mb-3" />
        <input type="submit" value="Login" class="btn btn-primary" />
    </form>
</div>